<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#ffffff"
    tools:context=".ManageMainActivity">

    <TextView
        android:id="@+id/topword"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="관리자 페이지입니다"
        android:textSize="30sp"
        android:gravity="center"
        android:textColor="@color/colorText"
        android:layout_marginTop="50sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="150sp"
        android:orientation="vertical"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/topword">
        <Button
            android:id="@+id/studentsList"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/colorMainButton"
            android:textColor="#ffffff"
            android:onClick="mClick"
            android:textSize="14sp"
            android:textStyle="bold"
            android:text="학생리스트"
            android:drawableLeft="@drawable/manager_stu"/>


        <Button
            android:id="@+id/reservationsList"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/colorRequestButton"
            android:textColor="@color/colorText"
            android:layout_marginTop="30sp"
            android:onClick="mClick"
            android:textSize="14sp"
            android:textStyle="bold"
            android:text="예약리스트"
            android:drawableLeft="@drawable/manager_res"/>

        <Button
            android:id="@+id/reservationincalendar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/colorAccent"
            android:textColor="@color/colorBackground"
            android:layout_marginTop="30sp"
            android:onClick="mClick"
            android:textSize="14sp"
            android:textStyle="bold"
            android:text="날짜별예약"
            android:drawableLeft="@drawable/board_cal"/>
    </LinearLayout>
</android.support.constraint.ConstraintLayout>